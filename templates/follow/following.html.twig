{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}
    <div id="container">
        <div id="users">
            {% if users.count %}
                {% include 'user/user.html.twig' %}
            {% else %}
                <p id="empty">Подписки не найдены</p>
            {% endif %}
        </div>
    </div>
{% endblock %}