{% for post in posts %}
    <div class="post">
        <div class="head">
            <a class="user" href="{{ path('profile', {'username': post.user.username}) }}">{{ post.user.username }}</a>
        </div>
        <div class="body">{{ post.text }}</div>
    </div>
{% endfor %}