{% for post in posts %}
    <div class="post">
        <div class="head">
            <a class="user" href="{{ path('profile', {'nickname': post.user.nickname}) }}">{{ post.user.nickname }}</a>
        </div>
        <div class="body">{{ post.text }}</div>
    </div>
{% endfor %}